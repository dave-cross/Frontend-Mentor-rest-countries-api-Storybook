function App() {
  return (
    <>
      <h1>Storybook for REST Countries API challenge</h1>
      <p>
        This was meant to be a demo app for using Storybook to get the _parts_
        of the "REST Countries API" challenge sorted before actually using them.
      </p>
      <p>
        Please run <code>npm run storybook</code> instead
      </p>
    </>
  );
}

export default App;
