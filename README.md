# Frontend Mentor - REST Countries API with color theme switcher

This was meant to be a demo app for using Storybook to get the _parts_ of the "REST Countries API" challenge sorted before actually using them.
